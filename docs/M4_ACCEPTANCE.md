# M4 建造、维修与木筏成长验收记录

> 当前状态：`DOING`
> 本次切片：结构真值、六类结构装配、多层移动/撞顶、独立损伤/修补、潮兜收集网、筏缘护甲与 v17 存档
> 版本：`0.14.5`
> 日期：2026-07-19

## 已形成的闭环

- 基础筏格、潮铸筏缘护甲、墙、板门、承重柱、楼梯、上层地板和斜顶进入统一八件建造选择；滚轮换件、`R` 旋转、`F` 切换合法层高，HUD 同步显示件型、方向、层高、成本和结构总数；
- 墙/门使用规范化共享边占位，地板/屋顶使用楼面占位，柱/楼梯使用格内占位；结构数、边界、重复占位和层高由领域层统一拒绝；
- 底层竖向结构必须有基础筏格，上层竖向结构必须有地板；地板/屋顶需要两点承重，墙、门、柱和楼梯落点可参与支撑；
- 拆除承重点前会验证全部后继结构，存在依赖时拒绝；鲨鱼咬毁基础筏格后按拓扑逐轮清除失去支撑的结构；
- 六类结构均由原创分件装配构成，包含交错木板、绑扎、钉件、门框/门扇、双梁七级楼梯和编叶斜顶；运行时按几何与材质合并为最多七个实例批次；
- 板门在世界中接受准星焦点与 `E` 开合，门框保持固定、门扇转动；关闭时阻挡玩家，打开后释放通道，并具有独立木铰/摩擦程序音层；
- 结构幽灵预览复用最终分件轮廓，合法、材料不足和结构无支撑使用不同状态；有效建造才扣材料和锤耐久，无效点击不产生隐藏成本；
- v14 保存结构稳定 ID、类型、格坐标、层高、方向、生命和开门状态；v15 继续保存经真实表面校验的筏上脚底层高，v14 玩家迁移到基础层，恶意悬空高度会吸附到同坐标真实表面或归零；
- 基础筏格、楼梯、上层地板与斜顶进入统一可行走表面采样；同一水平位置有多层时只选择当前脚底可跨越的最高层，不会从底层瞬移到楼板上；
- 四向楼梯按朝向从本格底部连续上升到相邻格下一层，视觉、承重落点、射线包围盒和行走方向一致；离开上层边缘会进入既有重力/入水链，上层跳跃只从上方落回真实表面；
- 墙、门、柱按玩家脚底层高过滤碰撞：底层玩家不会被上层墙阻挡，上层玩家会被对应上层结构阻挡；低矮生活/种植/研究设备不会隔着楼板阻挡玩家，帆桅和天线仍保留贯层碰撞；
- 楼板按最低承梁厚度、斜顶按连续屋脊坡面采样下表面；玩家上跳跨过底面时在碰撞帧截断垂直速度，保留水平惯性与既有落点状态，四向楼梯落点一侧留出明确入口，屋檐外不会产生假碰撞；
- 楼板与斜顶撞顶使用不同木质/纤维程序音层、少量向下尘屑和低幅镜头冲击；冻结诊断记录结构 ID、材质、世界高度与碰撞帧速度，避免浏览器门禁依赖瞬态截图；
- 六类结构按各自最大生命进入完整、受损、临界三档；实例颜色按木、深木、绳、金属和纤维分别变化，临界阶段以确定性轻微错位、下垂和缺口表达，但继续使用原七个实例批次；
- 深潮鲨只从外沿筏格选择可达结构，方向投影、层高和当前损伤共同参与确定性排序；受损目标必然优先，完整目标按概率与基础筏格竞争，结构归零会删除目标并按既有承重拓扑逐轮坍塌；
- 已进入受损阶段的外沿弱点会把载入后的首轮巡游从 34 秒缩短到 6.5 秒；每次结构/筏格撕咬都立即写入当前 v17，刷新不能清除伤害；
- 建造锤准星命中受损结构时进入专用修补态，HUD 显示结构名、生命、百分比和按材质定义的成本；扣料、生命增加、金色/材质碎屑、空间木裂/绳纤维音、锤耐久和保存只在实际修补成功后共同提交；
- 柱和楼梯的底层格位与设备占位共享冲突规则，避免同一空间出现不可解释的设备穿插；
- 潮兜收集网使用独立四向筏缘占位，不占整格设备位；承托筏格必须存在、外侧必须是水格，同一边不能重复，网床外侧水格会阻止基础筏格穿插扩建；
- 收集网只截获未被钩住的正式漂流物，失败/满包世界掉落不会被自动吞取；12 件容量支持确定性部分接收，拒收余量继续留在海面，靠近按 `E` 才转入背包；
- 持锤右键拆网会原子预判并返还套件和全部装载物，输入由网具先消费而不会继续拆基础；承托筏格意外消失时套件损失、网中物资回到池化海面掉落；
- 原创网具模型的木框、铁件、绳床和浮子按材质合批，货物、磨损绳和满载标记继续独立动画；放置、入网、收取和落海具有不同木框/绳索/水体程序音与粒子；
- 潮铸筏缘护甲只允许初装到健康外围筏格，消耗 1 金属锭和 2 废金属；原创盐蚀合金导轨、锈蚀角片和铆固件使用固定实例预算，扩建后仍随原筏格保留，不增加逐帧对象分配；
- 缘甲把承托筏格、同格暴露结构和边挂收集网的鲨鱼单次伤害从 34 降为 15；切到护甲项右键拆卸返还 1 金属锭和 1 废金属，普通基础拆除会先拒绝，避免隐藏吞料；
- 深潮鲨把受损收集网作为独立目标，预兆、HUD、准星提示、目标诊断、咬击音画和保存使用 `collectionNet` 事务类型；网具归零后套件损失、真实装载物回到池化海面掉落；
- 持锤注视受损网具按 `E` 消耗 1 漂木和 1 绳索修复 36 生命，成功后才磨损锤具并保存；右键拆网仍拥有输入优先权，不会穿透拆掉承托基础；
- v17 在既有收集网稳定 ID、边位、生命和装载物之上保存逐格 `reinforced`；v16 迁移为无护甲，非布尔伪造值归零，既有 v1-v15 迁移和网具清洗保持不变；
- 512×320 低高度布局保持交互提示、建造面板和快捷栏各 10px 间距，最长结构成本不会改变面板尺寸或遮挡输入提示。

## 自动与浏览器证据

- `npm test -- --run`：41 个测试文件、249 项测试通过；新增 v16->v17 缘甲迁移、外围安装/拆卸、55% 伤害换算、同侧网口筛选、收集网伤害/修补/毁坏放货和鲨鱼真实网具择靶覆盖，结构与收集网既有测试继续通过；
- `npm run build`：TypeScript 与生产构建通过；React 主入口、3D 世界和 Rapier 继续独立拆包；
- 最终构建 `building` 行为场景实际合上 `showcase-door`，随后滚轮选择木墙、旋转到东向并在 `(1,-1)` 放置；结构数 `10 -> 11`、漂木 `48 -> 45`、绳索 `24 -> 23`、锤耐久 `80 -> 79`，当前 v17 同帧写入；
- 同一场景切换到二层后，在没有上层地板的 `(1,-1)` 再次点击，运行时返回 `invalid`，结构仍为 11、材料与锤耐久不变；
- 门与墙瞄准使用运行时局部相机/目标几何迭代产生真实 `mousemove`，不通过直接改相机或 Store 绕过交互；
- `building` 视觉场景在 512×320 通过面板边界和相交断言，并生成 `artifacts/screenshots/building-structures-desktop.png`；当前 Termux 的 1024×640 整页 readback 仍可能占用合成器，因此行为和视觉分段执行；
- `building` 多层场景真实按住 `W` 从约 `0.03m` 楼梯底连续升到 `2.18m` 地板，写入当前 v17 后用全新浏览器上下文冷启动恢复到上层；随后真实 `Space` 跳跃并落回地板，按住 `S` 下楼回到脚底 `0m`，全过程 Context 和固定步保持有效；
- `building ceiling` 最终构建分别从楼板和斜顶屋脊下真实按 `Space`：命中 `ceiling-floor` / `ceiling-roof`，冻结世界高度 `2.248m` / `2.543m`，垂直速度均为 `0`，随后回落基础层；多层场景最终 `ceilingHits=0`，证明楼梯入口和上层跳跃未被误封；
- `building` 攻防场景让深潮鲨真实两次命中外沿木墙，生命 `75 -> 41 -> 7`，同格基础保持 `100`，当前 v17 每次同步写入；全新浏览器上下文恢复临界材质和 `7/110` 修补 HUD 后，三次真实锤击完成 `7 -> 51 -> 95 -> 110`，漂木 `12 -> 9`、锤耐久 `80 -> 77`；
- `collection-net` 场景真实从背包选择套件、瞄准外缘并固定在 `(0,-1,北)`；新浏览器上下文从该 v17 档重置漂流场，首件漂木被动入网后运行时与存档均为 `1/12`，装载截图的网床、货物、提示和 HUD 已人工复核；
- 同一场景按 `E` 清空网具，第三个浏览器上下文冷启动确认 1 张空网；切换耐久 80 的建造锤右键拆除后，v17 网数组归零、套件返还 1、锤耐久为 79，基础筏格保持不变；
- `perimeter-defense` 真实滚轮选择第八件并在 `(0,-1)` 健康外围筏格安装缘甲：金属锭 `2 -> 1`、废金属 `4 -> 2`、锤耐久 `80 -> 79`，v17 同帧保存 `reinforced=true`；
- 新浏览器上下文注入生命 52 的网具后，深潮鲨真实双咬 `52 -> 37 -> 22`，而非无甲的 `34` 点单次伤害；目标、运行时、事务日志和 v17 均记录 `collectionNet:defense-net:22:false`；
- 持锤 `E` 把网具 `22 -> 58`，漂木 `6 -> 5`、绳索 `4 -> 3`、锤耐久 `79 -> 78`；冷重载恢复缘甲和生命 58，切到护甲项右键拆卸后金属锭返到 2、废金属返到 3、锤耐久降到 77；
- 独立毁坏探针让无护甲生命 20 的网具被一口撕碎：网数组归零、`worldDropCount=1`、事务为 `collectionNet:defense-net:0:true`，存档掉落至少保留原有 2 漂木/1 纤维，并允许攻击前新增截获物一并落海；
- `perimeter-defense-visual` 在 1024×640 高画质固定展示三格缘甲、6/12 装载网具和第八件建造 HUD，生成 `artifacts/screenshots/perimeter-defense-visual-desktop.png`；像素门禁为 `variation=188`、`nonBlack=2880`、Context 健康，人工检查导轨/角片贴合、成本文本和底部/右侧 HUD 无重叠；
- 攻击阶段在当前软件 GLES 使用 10 倍 `performance.now()` 时间源驱动正式固定步并继续受 `maxSubSteps=12` 约束；冷启动、修补和两张对照截图使用原生时间，因此该门禁只证明正确性，不计入目标 GPU 攻防节奏或性能通过。

复现命令：

```sh
CAPTURE_ONLY=building BUILDING_PART=behavior CAPTURE_FAST=1 \
  DRIFTWAKE_URL=http://127.0.0.1:4180 npm run capture
CAPTURE_ONLY=building BUILDING_PART=visual \
  DRIFTWAKE_URL=http://127.0.0.1:4180 npm run capture
CAPTURE_ONLY=building BUILDING_PART=traversal \
  DRIFTWAKE_URL=http://127.0.0.1:4180 npm run capture
CAPTURE_ONLY=building BUILDING_PART=ceiling \
  DRIFTWAKE_URL=http://127.0.0.1:4180 npm run capture
CAPTURE_ONLY=building BUILDING_PART=damage \
  DRIFTWAKE_URL=http://127.0.0.1:4180 npm run capture
CAPTURE_ONLY=collection-net CAPTURE_QUALITY=low \
  DRIFTWAKE_URL=http://127.0.0.1:4180 npm run capture
CAPTURE_ONLY=perimeter-defense CAPTURE_FAST=1 CAPTURE_QUALITY=low \
  DRIFTWAKE_URL=http://127.0.0.1:4180 npm run capture
CAPTURE_ONLY=perimeter-destruction CAPTURE_FAST=1 CAPTURE_QUALITY=low \
  DRIFTWAKE_URL=http://127.0.0.1:4180 npm run capture
CAPTURE_ONLY=perimeter-defense-visual CAPTURE_QUALITY=high \
  DRIFTWAKE_URL=http://127.0.0.1:4180 npm run capture
```

## 目标真实 GPU 门禁

```sh
CAPTURE_ONLY=building CAPTURE_QUALITY=high npm run capture
CAPTURE_ONLY=building CAPTURE_QUALITY=low npm run capture
```

人工连续检查：

1. 八种建造件在 30/60 FPS 下滚轮切换、旋转、换层和连续放置均只提交一次，预览不抖动、不穿过手中锤具；楼梯上下行、转向和上层跳跃没有卡阶或强烈镜头突变，楼板/斜顶撞顶不会穿层、连震或丢失水平惯性；
2. 门框、门扇、墙、柱、楼梯、地板与斜顶在平静海面和风暴倾斜时始终附着同一筏体坐标，不漂到岛屿或海面；
3. 开门、关门、建造、拒绝和拆除音色可辨，连续操作不会叠出削波或盖过鲨鱼预兆；
4. 1280×720 与 1920×1080 高低画质下结构实例批次、阴影、透明预览和 HUD 没有异常跳变；
5. 保存、刷新和迁移后结构、方向、生命、开门状态和承重链一致，恶意无支撑结构不会进入世界；
6. 鲨鱼直接撕咬六类暴露结构或基础时，目标、损伤阶段、级联结果、碎屑、空间声音、通知和保存发生在同一事务边界；锤修后的生命、材料和耐久与 HUD 一致；
7. 多张收集网和缘甲在平静、转向和风暴倾斜时始终贴合对应筏格，装载物不沉入海面或漂到岛上；连续入网、受击、修补、收取和锤拆没有重复物资、事件穿透或音频削波。

## M4 剩余工作

- 将当前稳定滚轮条升级为不遮挡世界的类别/件型选择交互，并完成连续建造、替换和取消的人体工学验收；
- 实现结构落海持续表现，并完成缘甲成本/减伤、网具攻防和两层功能分区的无说明玩家平衡验收；
- 在目标真实 GPU 完成双画质结构画面、阴影、混音和长时间扩建性能门禁。
